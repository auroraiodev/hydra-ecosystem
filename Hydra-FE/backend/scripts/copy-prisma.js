// This script is no longer needed - TypeScript compiles the Prisma files
// Keeping it as a no-op for now in case we need it later
console.log('Prisma files are compiled by TypeScript, no copy needed');
