---
name: new-api
description: Create a new API service.
---

# new-api

1. Create in `lib/api/` as `<name>API.ts`.
2. Use `axios` or `fetch` with project base config.
3. Export CRUD methods as needed.
4. Update `CLAUDE.md`.
