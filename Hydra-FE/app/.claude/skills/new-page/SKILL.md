---
name: new-page
description: Scaffold a new Next.js page.
---

# new-page

1. Create in `app/(group)/[name]/page.tsx`.
2. Wrap content in `animate-page-enter`.
3. Use `useClient` only if hooks are needed.
4. Update `CLAUDE.md` metadata.
